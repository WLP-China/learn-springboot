<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muqing.dao.NurseDao">

    <sql id="where">
        <where>
            <if test="params.name != null and params.name != ''">
                and name = #{params.name}
            </if>
        </where>
    </sql>

    <select id="count" resultType="int">
        select count(1) from t_nurse t
        <include refid="where"/>
    </select>

    <select id="list" resultType="com.muqing.model.Nurse">
        select * from t_nurse t
        <include refid="where"/>
        ${params.orderBy}
        limit #{offset}, #{limit}
    </select>

    <update id="update">
        update t_nurse t
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="photo != null">
                photo = #{photo},
            </if>
            <if test="professionalTitle != null">
                professionalTitle = #{professionalTitle},
            </if>
            <if test="info != null">
                info = #{info},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            updateTime = now()
        </set>
        where t.id = #{id}
    </update>

</mapper>
